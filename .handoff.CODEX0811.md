# Handoff — NCCN Guidelines MCP

Executed: 2026-08-11 Asia/Shanghai
Completed: 2026-08-11 Asia/Shanghai

## State

- Branch: `feat/remake-to-agent-plugin`.
- Current tip contains the dual-site rebuild, three Darwin Skill optimizations,
  Skill contract tests, and the result-card HTML; use `git log -1` for the exact
  tip rather than copying a stale hash into this handoff.
- This handoff and the project `AGENTS.md` are part of the closeout commit.
- No push was requested or performed. No files were deleted or cleaned.

## Delivered and aligned

- Dual-site plugin package, Codex/Kimi manifests, bounded catalog/content stores,
  consent-gated China downloads, and page/chunk-bounded evidence retrieval.
- `skills/nccn-guidelines/SKILL.md` now encodes failure branches, a visible
  China consent checkpoint, and an anti-pattern blacklist. Its three paired
  Darwin rounds were kept by majority vote: `2-1`, `3-0`, `3-0 better`.
- `skills/nccn-guidelines/test-prompts.json` contains the three confirmed
  evidence prompts. `tests/test_skill_contract.py` guards the Skill workflow,
  safety markers, runtime neutrality, and size limit.
- User configuration and browser fallback remain documented in
  [README.md](README.md) and [README-zh.md](README-zh.md).
- Evaluation evidence remains under [tests/evals](tests/evals/); the HTML
  Darwin result card is `tests/evals/nccn-guidelines-result-card.html`.

## Runtime and verification

- `uv run --offline python -m unittest discover -s tests -p 'test_*.py'`: 20
  passing.
- `uv build --offline`: passing.
- Codex, generic MCP, and Kimi manifests: valid JSON.
- `git diff --check`: passing; runtime-neutrality scan: clean.
- Playwright 1.62.1 and Chromium 151.0.7922.34 are installed globally on this
  Mac. The result-card screenshot smoke test passed at 1800x2737 in `/tmp`.

## Important continuation facts

- Python Global discovery and China download-page parsing are implemented. A
  fresh Python China login remains blocked; no authenticated PDF was indexed,
  so the three evidence prompts remain `dry_run`, not completed medical E2E.
- Direct browser and Kimi Computer Use controls downloaded one permitted China
  PDF in an already-authorized browser session. This is acquisition-only:
  browser cookies are never imported and the file is not automatically indexed.
- Preserve one-record China consent with `confirm_license=true`; do not bulk
  probe, retry `download-log`, silently switch sources, or log credentials,
  cookies, CSRF values, or issued URLs.

## Pending and residue

- `pending`: a real Python China E2E needs site-accepted authentication or an
  explicitly supplied session cookie, followed by a restarted MCP process.
- `not-applicable`: no deployment, PR, production service, or live product
  surface is part of this closeout; no deploy/live-verified claim is made.
- `out-of-scope`: ignored historical files `task_plan.md`,
  `implementation_report.md`, `notes.md`, local downloads/logs/caches, and
  `.DS_Store` were not deleted. They remain available for user review.
- The user-created `.handoff.CODEX0811.md` is now aligned and tracked; no
  cleanup confirmation is being requested or assumed.

## Suggested next work

1. If real China Python E2E is required, configure accepted credentials or an
   explicit session cookie only in the MCP process, restart the host, and run
   one consented record with safe outcome logging.
2. If browser fallback becomes a product feature, design an explicit,
   user-approved ingestion boundary; never read browser cookies or accept
   arbitrary local PDF paths.
3. Before another multi-step change, inspect this handoff, `AGENTS.md`,
   `rebuild_nccn_mcp_SPEC.md`, and current Git state.

## Relevant skills

- `web-access` for live NCCN/OpenAI/Kimi research.
- `tdd` and `verification-loop` for adapter or authentication changes.
- `handoff` or `neat-freak` after another multi-step follow-up.
